# Todo
* fix instructions modal. should use a separate local_storage key, hideInstructions. If absent or set to false, show
modal on page load, otherwise skip it.
* Add customizations for share text, headerLineEnd, afterGrid. if headerLineEnd append space plus string. if afterGrid
new line plus string